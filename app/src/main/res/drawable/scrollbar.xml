<shape android:shape="rectangle"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:centerColor="@color/scrollbar"
        android:endColor="@color/scrollbar"
        android:startColor="@color/scrollbar" />
    <corners android:radius="20dp" />
</shape>